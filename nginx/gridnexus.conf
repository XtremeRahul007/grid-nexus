server {
    listen 80;
    server_name gridnexus.localhost;

    root /var/www/grid-nexus;
    index index.html;

    location / {
        try_files $uri $uri/ /index.html;
    }

    location /auth/ {
        try_files $uri $uri/ /auth/index.html;
    }
}
